machine:
  environment:
    PIP_DISABLE_PIP_VERSION_CHECK: true
    DOKKU_HOST: techb.us
    DOKKU_APP_NAME: rtoot
    GIT_AUTHOR_NAME: Paul McLanahan
    GIT_AUTHOR_EMAIL: pmclanahan@gmail.com

test:
  override:
    - lektor project-info

deployment:
  prod:
    branch: master
    owner: pmac
    commands:
      - ./deploy.sh
